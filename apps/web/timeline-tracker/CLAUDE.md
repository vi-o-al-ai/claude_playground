# Timeline Tracker

Real-time activity timeline tracker. All logic is inline in a single `index.html` (~15KB).

- localStorage for persistence.
- Export/import JSON for data portability.
- No external dependencies, no `@arcade/shared-ui`.
