/**
 * Stub for the art customizer UI.
 * Fleshed out in the next commit — see the asset-customization overlay.
 */
export function initArtCustomizer(_assetStore) {
  // no-op placeholder
  window._showArtCustomizer = () => {
    console.warn("Art customizer UI not yet wired up.");
  };
}
