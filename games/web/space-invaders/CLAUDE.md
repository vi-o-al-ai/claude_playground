# Space Invaders

Canvas-based arcade shooter. Unlike other web games, this does NOT use DOM rendering -- it uses HTML5 Canvas with `requestAnimationFrame` for a real-time game loop.

- `engine.js` -- game logic with pixel art sprite definitions.
- `ui.js` -- canvas rendering.
