# Arcade Hub

Landing page that links to all games in the arcade. No game logic.

- Includes PWA service worker (`public/sw.js`) for offline support.
- No `@arcade/shared-ui` dependency; no tests.
- When adding a new game, add a game card link in `index.html`.
