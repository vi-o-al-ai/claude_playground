# Step 4: Unit Testing with Vitest

## Overview

Unit tests have been added for the Sudoku and Tic-Tac-Toe game engines using **Vitest**. All tests run in under 300ms with zero DOM dependencies.

## Test Results

```
 Test Files  3 passed (3)
      Tests  77 passed (77)
   Duration  286ms
```

## Test Coverage

### Sudoku Engine (`packages/sudoku/src/__tests__/engine.test.js`) — 10 tests

- **`isValid`** — row, column, and 3x3 box constraint validation
- **`clone`** — deep copy correctness
- **`getCandidates`** — candidate number computation for empty cells
- **`encode/decode`** — puzzle sharing round-trip, error handling

### Sudoku Game State (`packages/sudoku/src/__tests__/game-state.test.js`) — 37 tests

- **`createGameState`** — default values
- **`selectCell`** / **`toggleNotes`** — UI state toggling
- **`enterNumber`** — correct moves, mistakes, game over after 3 mistakes, notes mode, win detection, related notes clearing
- **`erase`** — clearing values, protecting given cells
- **`undo`** — reversing moves, restoring notes, mistake counter rollback
- **`useHint`** — filling cells, decrementing counter, random fallback
- **`moveSelection`** — arrow key navigation, edge clamping
- **`getHighlightInfo`** — row/column/box highlighting, same-number detection
- **`getNumpadCompletion`** — number completion status
- **`formatTime`** — timer string formatting
- **`serializeState/deserializeState`** — save/load round-trip
- **`loadSharedPuzzle`** — URL puzzle loading, error handling
- **`shareUrl`** — share URL generation
- **`checkWin`** — win condition detection

### Tic-Tac-Toe Engine (`packages/tic-tac-toe/src/__tests__/engine.test.js`) — 30 tests

- **`WIN_LINES`** — all 8 winning combinations present
- **`createGameState`** — fresh state initialization
- **`checkWin`** — horizontal, vertical, diagonal, anti-diagonal, no-win cases
- **`isBoardFull`** — empty, partial, full board detection
- **`makeMove`** — placement, turn alternation, invalid moves, win/draw detection, score tracking
- **`findWinningMove`** — row, column, diagonal winning moves, no-move case
- **`computeComputerMove`** — win priority, blocking, center/corner/any fallback strategy
- **`restart`** — board reset with score preservation

### Shared UI (`packages/shared-ui/src/__tests__/`) — 35 tests

- **`modal.test.js`** (13 tests) — DOM creation, show/hide lifecycle, button rendering with variants, backdrop and Escape dismiss, auto-close behavior, HTML message support, style deduplication, destroy cleanup
- **`game-header.test.js`** (9 tests) — Title display, arcade back link, custom URL, toggle settings with onChange, initial checked state, multiple settings, container mounting, setTitle update
- **`game-over.test.js`** (13 tests) — Overlay visibility, stats display, restart callback with auto-hide, default/custom restart label, extra buttons, re-show clears previous state, no-op hide, destroy cleanup

## TDD Workflow

All new features and components **must** follow test-driven development:

1. **Write failing tests first** — commit them so the test expectations are reviewable on their own
2. **Implement the code** — commit the implementation that makes the tests pass
3. **Refactor if needed** — clean up while keeping tests green

This ensures tests define the intended behavior before implementation influences them, and the git history clearly separates test design from implementation.

## Fixture-Based Testing

Sudoku tests use a **pre-built puzzle fixture** (`fixtures.js`) instead of calling the puzzle generator at runtime. This avoids the expensive backtracking solver, keeping the entire test suite under 300ms.

```js
// fixtures.js — a known-valid Sudoku puzzle and solution
export const SOLUTION = [[5, 3, 4, 6, 7, 8, 9, 1, 2], ...];
export const PUZZLE = [[5, 3, 0, 0, 7, 0, 0, 0, 0], ...];
export const EMPTY_CELLS = [{ r: 0, c: 2 }, ...];
```

### What's Not Tested (and Why)

- **`solve()`** and **`countSolutions()`** — These use exhaustive backtracking that takes seconds to minutes depending on board state. They are validated indirectly: the fixture puzzle was generated by these functions, and all game-state tests verify correct behavior against the fixture's known solution.
- **`generate()`** — Calls both `solve()` and `countSolutions()` internally. Same performance concern. Could be tested in a separate slow/integration test suite if needed.

## Usage

```bash
# Run all tests
npm test

# Run tests in watch mode
npm run test:watch

# Run tests for a specific package
npx vitest run packages/tic-tac-toe
npx vitest run packages/sudoku

# Run a single test file
npx vitest run packages/sudoku/src/__tests__/engine.test.js
```

## npm Scripts

| Script               | Command              | Description             |
| -------------------- | -------------------- | ----------------------- |
| `npm test`           | `vitest run`         | Run all tests once      |
| `npm run test:watch` | `vitest`             | Run tests in watch mode |
| `npm run check`      | lint + format + test | Full CI check           |
